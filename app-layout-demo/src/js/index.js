let controlItemList = document.getElementsByClassName("control-item");

let setControlItemActive = (controlItem) => {
    if (!controlItem.classList.contains("active")) {
        controlItem.classList.add("active");
    }
    else{

    }
}

